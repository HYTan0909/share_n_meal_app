import 'package:flutter/material.dart';

class RideDriverHistoryPage extends StatelessWidget {
  const RideDriverHistoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('This is a history page')),
    );
  }
}
